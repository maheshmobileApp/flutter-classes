import 'package:flutter/material.dart';

class StelessWidget extends StatelessWidget {
  const StelessWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
  
    return Container(child: Text("hello vamsi"));
  }
}

/*
class className extends StatelessWidget {

 className()

 @ovveride
 Widget build(BuildContext context) {

   return Widget();
 }

}

 */